// ─── SUPABASE CLIENT ───
// This file will be configured once you create your Supabase project.
// For now, it exports placeholder functions so the app compiles.
//
// To set up:
// 1. Go to https://supabase.com and create a new project
// 2. Copy your project URL and anon key
// 3. Create a .env.local file in the project root with:
//    NEXT_PUBLIC_SUPABASE_URL=your-project-url
//    NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
// 4. npm install @supabase/supabase-js @supabase/ssr
// 5. Uncomment the code below and remove the placeholders

/*
import { createBrowserClient } from "@supabase/ssr";

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
*/

// Placeholder export so imports don't break
export const supabaseConfigured = false;
